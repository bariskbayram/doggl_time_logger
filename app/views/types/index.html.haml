- if notice
  %p#notice.callout.warning.margin-top-2= notice

%h1.text-center="Types"

%table.hover
  %thead
    %tr
      %th="Description"
      %th{:colspan => "2"}
  = turbo_stream_from "types"
  %tbody#types
    = render @types


= link_to 'New Type', new_type_path, class: "button primary"
